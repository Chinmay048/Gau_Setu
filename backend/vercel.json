{
  "version": 2,
  "buildCommand": "npm install",
  "installCommand": "npm install",
  "env": {
    "NODE_ENV": "production"
  },
  "envDescription": {
    "JWT_SECRET": "JWT secret for token generation (set in Vercel dashboard)",
    "FRONTEND_URL": "Frontend domain for CORS (set in Vercel dashboard)"
  },
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.js"
    }
  ]
}
